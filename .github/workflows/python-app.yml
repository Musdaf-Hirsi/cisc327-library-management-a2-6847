name: python-app (placeholder)

# This workflow was removed at the user's request. Kept as a placeholder briefly
# so the deletion can be performed reliably by the agent. Remove this file
# manually or let the agent delete it in the next step.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Placeholder: workflow removed."
